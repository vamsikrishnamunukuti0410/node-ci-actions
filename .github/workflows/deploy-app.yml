name: deploy-app
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  first-job:
    runs-on:  ubuntu-latest
    
    steps:
      - name: deployment
        run: echo "Deploying app..."

# Updating workflow
